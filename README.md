# study_C
**바닥부터 시작하는 C언어 공부 기록용**

> **2026-04-04 자판기 만들어보기**
> * while, for, if, switch case, 삼항연산자
> * vendingmachine.c

> **2026-04-05 배열 알아보기**
> * 배열, SIZE, 2차원 배열

> **2026-04-06 함수 사용해보기**
> * 함수 구조에 대한 공부가 더 필요하다.
> * int, void return값 유무에 따라 선택.

> **2026-04-06 포인터 알아보기**
> * 정리가 잘 안된다.
> * 함수 밖의 변수에 접근하기 위해 포인터를 사용한다.
> * &는 주소값을 의미.
> * *는 두가지 의미를 가진다.
> * 포인터 변수를 선언하는 것(표식)과 해당 주소에 가서 값을 저장하는 것(동작)을 잘 구분하자. (생성과 역참조)
> * int *ptr 는 쪽지 생성, *ptr = 10 은 그 주소에 값을 저장
> * ptr 는 이미 주소값 자료형이다.
> * 포인터 변수(주소 쪽지)를 만드려면 실제 변수(집)가 있어야한다.
> * int atk = 0;
> * int *ptr = &atk;
> * pikachu.c

> **2026-04-08 포인터, 함수 연습**
> * 함수 구조와 포인터 사용에 익숙해질 필요가 있음
> * 피카츄 관리 시스템 만들어봄
> * 아직 구조 파악에 시간이 걸림

> **2026-04-12 백준 단계별로 풀어보기**
> * 입출력, 조건문 완료
> * 반복문 풀이 중

> **2026-04-13**
> * 모든 배열 이름은 첫번째 원소의 주소값을 나타낸다 (문자열이 아니어도)
> * scanf로 입력을 받을 때, 공백은 각 원소를 구분하는 역할을 한다. %d 처럼 지정해준 서식이 나올 때까지 읽고, 공백이 나오면 다음으로 넘어간다. 엔터와 스페이스는 같은 공백으로 인식한다.
> * 배열 크기를 scanf로 입력받아서 지정 가능. 하지만 초기화를 동시에 하지는 못한다.
> * 변수에 담기는 값과 크기 지정 등 순서 생각해보기 -> 순서대로 실행하기 때문
> * 전역 변수 / 지역변수
> * 데이터영역 / 스택영역
> * 자동 초기화(data에 값 지정된 변수 저장/BSS에 선언만 한 변수 저장. 그럼 0으로 초기화) / 쓰레기값 있어서 초기화 필요
> * 백준 채점 시스템. 입력값과 출력값은 따로 저장된다. 입력을 받자마자 출력해도 서로 다른 곳에 적히기 때문에 입력과 출력의 순서를 관리할 필요가 없다. (실시간 처리 streaming)
> * 데이터를 저장할 필요가 없다면 하지 말자. 다시 확인이 필요한 데이터 집합이 아니라면 값을 비교하고 바로 저장하거나 바로 출력하는 것이 효율적이다.
> * 최대, 최솟값 찾기에서 초기값 설정에 신경쓰자.
> * 등호나 부등호에 따라 달라지는 점도 확인 필요.
> * 배열 인덱스 번호 주의하기. (다시 확인 필요)
> * 변수는 필요한 시점이나 필요한 곳에 가깝게 선언해서 사용하자.
